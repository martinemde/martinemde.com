node_modules

# Output
.output
.vercel
.netlify
.wrangler
.cloudflare
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
# Follow Vite conventions:
# - .env and .env.[mode] are committed (shared defaults)
# - .env.local and .env.[mode].local are ignored (local overrides with secrets)
.env.local
.env.*.local
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code scratch worktrees
.claude/worktrees/
